# GamjaPick Roadmap

## 현재 구조

GamjaPick은 사람이 카드뉴스 주제를 찾고, 한 장의 4:5 카드뉴스로 완성하는 개인용 도구다. 초기 운영 비용은 0원이며 DB·계정·유료 API·생성형 AI 수집은 사용하지 않는다.

## Stage 1 — Topic Radar

### 수집 원칙

- 일반 최신글·기사 본문이 아니라 사이트가 직접 선정한 인기·공유·많이 본·이슈 목록만 수집한다.
- 공개 목록의 제목, URL, 순위, 목록 유형, 카테고리, 공개된 시간·추천·댓글·목록 요약만 저장한다.
- RSS는 해당 신호를 보존하는 경우에만 사용한다. 현재 대상 신문사의 RSS는 최신 기사 피드라서 사용하지 않는다.
- 목록 단위 collector가 독립적으로 성공·실패한다. 401/403/429 또는 목록 selector 불일치는 우회하지 않고 그 목록만 실패 처리한다.

### 수집 대상과 완료 기준

| Source | 목록 | 구현 상태 |
| --- | --- | --- |
| Dogdrip | 개드립/유저 개드립/읽을 거리 판 인기글 | 구현 완료. 공개 요청이 차단되면 독립 실패 처리 |
| 동아일보 | 실시간 인기순·공유순, 종합/경제/국제/사회 TOP 5 | 구현 완료 |
| 한국경제 | 사회·테크 많이 본 뉴스 TOP 5 | 구현 완료 |
| 경향신문 | 지금 많이 보는 기사, 이슈 기사 업데이트순 | 구현 완료 |
| MBC 뉴스 | 엠빅 X 14F, 많이 본 뉴스 포털·SNS | 구현 완료 |

`data/current.json`에는 최신 목록의 항목, `data/recent.json`에는 최근 48시간의 고유 항목을 저장한다. 각 항목은 `firstSeenAt`, `lastSeenAt`, `seenCount`, `consecutiveCount`, `isCurrent`, 현재 metrics, delta, 최대 48개 history를 가진다. 동일 원문이 한 source의 복수 목록에 있으면 `placements`에 노출 위치를 병합한다.

GitHub Actions는 30분 cron과 `workflow_dispatch`로 `npm run collect`를 실행하고, 변경된 JSON만 자동 커밋한다. Topic Radar는 collector를 직접 실행하지 않으며 생성된 JSON만 표시한다.

### 대시보드 완료 기준

- 현재 목록·최근 48시간 탭과 source 필터 제공
- 출처, 목록 유형, 카테고리, 순위, 포착 지속성, 공개 지표와 변화량 표시
- 원문으로 이동하고 제목·출처를 Card Editor로 전달 가능
- 한 목록 실패가 다른 source 데이터 사용을 막지 않음

## Stage 2 — Card Editor

`/editor`에서 사진과 문구를 입력해 1080 × 1350(4:5) 카드 표지를 만든다.

- 로컬 이미지 업로드·드래그 앤 드롭·클립보드 붙여넣기
- 제목·부제·출처의 폰트, 크기, 굵기, 줄간격, 정렬, 단어 강조 색상, 위치 조절
- 이미지 확대·이동, 하단 그라데이션 조절
- Topic Radar에서 제목·출처 전달
- 브라우저 Canvas로 PNG 저장

이미지와 편집 상태는 브라우저 안에만 남으며, 서버 이미지 저장·계정·DB는 사용하지 않는다.

## 이후 검토

실사용 후에만 동일 사건 묶기, 제외 키워드, 사용자 정의 정렬, 추가 템플릿, 로컬 편집 상태 저장을 검토한다. 자동 사실 검증, 자동 게시, 생성형 AI 기반 수집·랭킹은 현재 범위가 아니다.
