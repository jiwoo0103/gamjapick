# ROADMAP.md

# GamjaPick Roadmap

## 1. Project Goal

GamjaPick은 카드뉴스 제작을 위해 필요한 **주제 탐색부터 최종 이미지 편집까지** 한곳에서 처리하는 개인용 웹 도구다.

개발은 크게 두 단계로 진행한다.

1. **Stage 1 — Topic Radar**
   - 국내외 인기·급상승 주제를 자동 수집
   - 최근 반응 변화와 지속성을 확인할 수 있는 대시보드 제공
   - 사용자가 직접 카드뉴스로 만들 주제를 선택할 수 있게 함

2. **Stage 2 — Card Editor**
   - 사용자가 이미지와 문구를 입력하면 카드뉴스 템플릿에 자동 배치
   - 자동 배치 후 폰트, 크기, 위치 등을 직접 수정
   - 최종 결과를 PNG로 저장

Stage 1이 실제 운영 가능한 상태가 된 뒤 Stage 2를 시작한다.

---

# Stage 1 — Topic Radar

## 목표

뉴스뿐 아니라 커뮤니티에서 화제가 되는 글, 황당하지만 실제로 있었던 사건, 해외 바이럴·오프비트 이슈, 실시간 검색 트렌드까지 한 화면에서 확인할 수 있게 한다.

수집 단계에서는 **생성형 AI를 사용하지 않고**, **DB와 유료 API 없이** 동작하도록 한다.

## 1-1. 프로젝트 기반 + 수집 가능성 검증

Next.js + TypeScript + Tailwind CSS 기반으로 프로젝트를 구성한다.

초기 수집 대상은 다음 6개다.

### 국내

- DCInside 실시간 베스트
- FMKorea 포텐
- TheQoo HOT

### 해외

- Reddit `r/nottheonion`
- NDTV Offbeat

### 트렌드 센서

- Google Trends KR

각 소스에 대해 실제 데이터를 가져올 수 있는지 먼저 검증한다.

확인할 항목:

- 제목
- 원문 URL
- 게시 시각 또는 확인 가능한 시간 정보
- 조회수
- 추천·좋아요
- 댓글 수
- 기타 공개적으로 제공되는 반응 지표

소스마다 제공하지 않는 값은 추측하지 않고 `null`로 처리한다.

공식 API 또는 RSS가 있으면 우선 사용한다. HTML 수집이 필요한 경우 공개 페이지를 낮은 빈도로 요청한다.

403, 429, CAPTCHA, 로그인 요구 등 차단이 확인되면 이를 우회하지 않는다. 해당 collector만 실패 처리하고 다른 소스는 계속 실행되게 한다.

해외 제목 번역은 이 단계에서 실제 사용 가능성도 확인한다.

- 원문 제목은 항상 보존
- 한국어 번역은 별도 필드로 저장
- 번역 실패 시 원문으로 정상 표시 가능해야 함
- 초기 번역 방식은 무료 로컬 번역 도구인 Argos Translate를 우선 검토

### 완료 기준

- 6개 source별 collector의 성공/실패 상태를 명확히 확인할 수 있음
- 성공한 collector에서 실제 제목과 URL을 수집할 수 있음
- 하나의 collector가 실패해도 전체 실행이 중단되지 않음
- 해외 제목의 원문 보존과 한국어 번역 흐름이 동작함
- 차단된 사이트에 대한 우회 로직이 없음

### 구현 상태 (2026-08-19)

- Next.js App Router + TypeScript + Tailwind 기반과 TypeScript collector 공통 계약을 구성했다.
- DCInside, FMKorea, TheQoo는 현재 공개 HTML을, Reddit은 공개 Atom RSS를, NDTV는 공식 Offbeat RSS를, Google Trends KR은 공식 RSS를 사용한다.
- 각 source는 독립 성공·실패 결과를 반환한다. Reddit은 실행 시점에 따라 429가 발생할 수 있으며, 우회하지 않고 해당 source만 실패 처리한다.
- 영어 원문은 `titleOriginal`에 보존하고, 무료 로컬 Argos Translate의 영어→한국어 모델 결과를 `titleKo`에 저장한다. 번역 실패 시 원문을 유지한다.
- `current.json`·`recent.json` 누적과 30분 GitHub Actions 수집은 1-2에서 구현한다.

---

## 1-2. 데이터 누적 + 30분 자동 수집

수집 가능성이 검증되면 모든 collector의 출력을 공통 Topic 구조로 정규화한다.

DB는 사용하지 않고 다음 파일을 상태 저장소로 사용한다.

```text
data/
├─ current.json
└─ recent.json
```

### `current.json`

가장 최근 수집 시점에 각 source의 인기 목록에 존재하는 항목을 저장한다.

### `recent.json`

최근 48시간 동안 발견된 고유 항목을 저장한다.

각 항목에서는 최소한 다음 상태를 관리한다.

- source
- 원문 ID 또는 안정적인 식별자
- 원문 URL
- 제목
- 해외 항목의 `titleOriginal`
- 해외 항목의 `titleKo`
- 게시 시각
- `firstSeenAt`
- `lastSeenAt`
- `isCurrent`
- `seenCount`
- `consecutiveCount`
- 현재 반응 지표
- 직전 관측 대비 변화량
- 반응 지표 history

### 상태 규칙

- `seenCount`: 인기 목록에서 총 몇 번 발견됐는지 누적
- `consecutiveCount`: 연속 수집에서 몇 번 계속 등장했는지
- 목록에서 사라지면 `isCurrent=false`, `consecutiveCount=0`
- 나중에 다시 등장하면 `seenCount`는 계속 누적하고 `consecutiveCount=1`
- `recent.json`에서는 마지막 발견 후 48시간이 지난 항목 제거
- history는 최대 48개 snapshot 유지
- 30분 간격 기준 최대 약 24시간의 반응 변화 확인 가능
- 반응 지표가 없는 source에서는 없는 값을 임의 계산하지 않음

다음 상태 전이를 테스트한다.

- 최초 등장
- 연속 등장
- 목록에서 사라짐
- 다시 등장
- 48시간 경과 후 제거
- history가 48개를 넘을 때 오래된 snapshot 제거
- 조회·추천·댓글 등의 delta 계산

### 자동 실행

GitHub Actions를 이용해 30분마다 collector를 실행한다.

흐름:

```text
GitHub Actions
    ↓
각 source collector 실행
    ↓
공통 형식으로 정규화
    ↓
current.json 갱신
    ↓
recent.json 병합 및 정리
    ↓
대시보드에서 사용
```

`workflow_dispatch`를 제공해 필요할 때 수동 실행도 가능하게 한다.

GitHub Actions가 몇 분 늦게 시작될 수 있다는 점을 허용하고, 정확히 정각 실행되어야 하는 구조로 만들지 않는다.

### 완료 기준

- 실제 수집 결과가 `current.json`과 `recent.json`에 정상 저장됨
- 위 상태 전이 테스트가 통과함
- history와 delta가 정상적으로 누적됨
- 48시간이 지난 항목이 제거됨
- GitHub Actions에서 30분 주기 실행 가능
- 한 source 실패가 전체 데이터 갱신을 막지 않음

### 구현 상태 (2026-08-19)

- `data/recent.json`을 기준으로 48시간 이력, 현재 여부, 연속 포착 횟수, 지표 변화량, 최대 48개 history를 병합한다.
- `data/current.json`은 최신 수집에서 현재 인기인 항목만 저장한다.
- `npm run collect`는 두 상태 파일을 갱신하고, `npm run collect:check`은 파일을 바꾸지 않는 dry-run이다.
- `collect-topics.yml`은 30분 cron과 수동 실행을 제공하며, 바뀐 데이터 파일만 자동 커밋한다.

---

## 1-3. Topic Radar 대시보드 완성

수집된 데이터를 사람이 빠르게 훑고 카드뉴스 후보를 고를 수 있는 화면을 만든다.

Next.js는 수집기를 직접 실행하지 않고 만들어진 JSON 데이터만 표시한다.

### 기본 화면

상단 탭:

- `현재 인기`
- `최근 48시간`

source 필터:

- 전체
- DCInside
- FMKorea
- TheQoo
- Reddit
- NDTV
- Google Trends

각 항목에는 가능한 범위에서 다음을 표시한다.

- source
- 한국어 제목
- 해외 항목의 원문 제목
- 게시 시각
- 최초 발견 시각
- 마지막 발견 시각
- 현재 여부
- 조회수
- 추천·좋아요
- 댓글
- 직전 수집 대비 변화량
- `seenCount`
- `consecutiveCount`
- 원문 링크

Google Trends처럼 일반 게시글과 구조가 다른 source는 억지로 동일한 지표를 만들지 않고 해당 source에 맞는 정보를 보여준다.

### 정렬

초기에는 복잡한 HOT 점수를 만들지 않는다.

우선 다음처럼 사람이 이해하기 쉬운 정렬만 제공한다.

- 최근 발견 순
- 연속 포착 횟수
- 조회수 변화량
- 추천·좋아요 변화량
- 댓글 변화량

해당 metric이 없는 source는 해당 정렬에서 자연스럽게 뒤로 처리한다.

### 초기 버전에서 하지 않는 것

- 생성형 AI 자동 평가
- AI 기반 주제 추천
- 자동 사실 검증
- 복잡한 HOT Score
- 의미 기반 중복 제거
- 로그인
- DB
- 저장 목록
- GPT 연결
- 자동 카드뉴스 생성
- 자동 인스타 게시

실제 데이터를 3~7일 사용해본 뒤 필요성이 확인된 기능만 추가한다.

### 구현 상태 (2026-08-19)

- Next.js 첫 화면은 collector를 실행하지 않고 `data/current.json`과 `data/recent.json`만 정적으로 읽는다.
- `현재 인기`와 `최근 48시간` 탭, 6개 source 필터, 최근 발견·연속 포착·조회수/추천/댓글 변화 정렬을 제공한다.
- 카드에는 source, 한국어 제목과 해외 원문, 게시/발견 시각, 현재 여부, 공개 지표와 변화량, 포착 횟수, 원문 링크를 표시한다.
- Google Trends는 검색량과 그 변화량만 별도로 표시하며, 없는 지표는 만들지 않는다.

### Stage 1 완료 기준

다음 상태가 되면 Stage 1을 완료한 것으로 본다.

- 30분 자동 수집이 안정적으로 실행됨
- 현재 인기와 최근 48시간 항목을 웹에서 확인 가능
- source별 필터가 가능
- 반응 변화와 연속 등장 여부를 사람이 판단 가능
- 해외 항목을 한국어 제목 중심으로 볼 수 있음
- 원문으로 바로 이동 가능
- 특정 collector 실패 시 다른 source 데이터는 계속 사용 가능
- 무료 운영 구조가 유지됨

---

# Stage 2 — Card Editor

## 목표

카드뉴스 후보를 정한 뒤 Canva 같은 범용 디자인 도구를 처음부터 조작하지 않아도 되도록 한다.

사용자가 **사진 + 문구**를 넣으면 기본 카드뉴스 템플릿을 자동으로 완성하고, 필요한 부분만 직접 수정한 뒤 PNG로 저장할 수 있게 한다.

초기 편집기는 **인스타그램 4:5 표지 한 장 제작**에 집중한다.

---

## 2-1. 자동 템플릿 편집기

경로 예시:

```text
/editor
```

사용자가 입력하는 기본 값:

- 배경 이미지
- 제목
- 선택적 부제
- 필요 시 출처 문구

입력 즉시 4:5 카드뉴스 템플릿에 자동 배치한다.

### 기본 자동 처리

- 4:5 캔버스 유지
- 업로드 이미지 자동 채우기
- 제목 영역 기본 위치 적용
- 제목 길이에 따른 기본 글자 크기 조정
- 자연스러운 자동 줄바꿈
- 하단 텍스트 가독성을 위한 기본 그라데이션
- 템플릿의 기본 여백과 안전영역 유지

자동 배치 결과는 확정값이 아니라 사용자가 수정할 수 있는 시작점이다.

### 직접 수정 가능한 항목

- 폰트
- 폰트 크기
- 굵기
- 줄간격
- 글자 정렬
- 줄바꿈
- 텍스트 위치
- 이미지 확대·축소
- 이미지 위치 이동
- 그라데이션 강도

초기 버전은 템플릿 하나를 완성도 높게 만드는 것을 우선하며, Canva처럼 모든 요소를 자유 배치하는 범용 편집기는 만들지 않는다.

### 구현 원칙

가능한 편집 처리는 브라우저 안에서 수행한다.

초기 버전에서는 다음을 요구하지 않는다.

- 이미지 서버 업로드
- 사용자 계정
- DB
- 클라우드 스토리지
- 유료 이미지 처리 API

### 완료 기준

- 이미지와 제목을 입력하면 즉시 기본 카드뉴스가 만들어짐
- 자동 줄바꿈과 기본 크기 조정이 동작함
- 폰트와 주요 텍스트 속성을 변경할 수 있음
- 이미지 위치와 크기를 수정할 수 있음
- 새로고침 전까지 안정적으로 편집 가능
- 외부 서버 없이 기본 편집 기능이 동작함

---

## 2-2. Topic Radar 연결 + PNG 출력

Stage 1에서 발견한 주제와 편집기를 연결한다.

Topic Radar의 항목에서 나중에 다음 흐름으로 이동할 수 있게 한다.

```text
주제 발견
   ↓
카드 만들기
   ↓
Editor
   ↓
이미지 + 제목 확인
   ↓
자동 배치
   ↓
직접 수정
   ↓
PNG 저장
```

초기에는 주제 제목을 Editor로 전달하는 정도로 시작한다.

사용자가 원하는 최종 제목과 이미지는 직접 선택할 수 있어야 하며, 수집한 커뮤니티 제목을 그대로 카드뉴스 제목으로 강제하지 않는다.

### PNG 출력

편집 결과를 카드뉴스용 PNG로 내보낸다.

출력 시 확인할 것:

- 4:5 비율 유지
- 글자 깨짐 없음
- 선택한 폰트 정상 반영
- 화면 미리보기와 결과 이미지의 배치 차이 최소화
- 업로드 이미지 품질을 불필요하게 낮추지 않음

### Stage 2 완료 기준

- Topic Radar에서 Editor로 자연스럽게 이동 가능
- 사진과 제목을 넣으면 템플릿이 자동 생성됨
- 폰트·크기·위치·이미지를 직접 수정 가능
- 최종 결과를 PNG로 저장 가능
- DB나 유료 API 없이 기본 워크플로 전체가 동작함

---

# Final Workflow

GamjaPick의 최종 기본 사용 흐름은 다음과 같다.

```text
[자동]
국내외 source 30분 수집
        ↓
current.json / recent.json
        ↓
Topic Radar

[사용자]
주제 확인
        ↓
카드뉴스로 만들 주제 선택
        ↓
Editor
        ↓
사진 + 최종 문구 입력
        ↓
자동 템플릿 배치
        ↓
필요한 부분만 직접 수정
        ↓
PNG 저장
```

GamjaPick은 초기에는 **주제를 대신 판단하거나 콘텐츠를 자동 게시하는 시스템이 아니라, 사람이 좋은 주제를 빠르게 발견하고 카드뉴스를 빠르게 완성하도록 돕는 도구**로 유지한다.

---

# Later Improvements

Stage 1과 Stage 2가 실제 사용에서 안정된 뒤에만 검토한다.

후보:

- 카드뉴스 후보 저장
- 같은 사건의 중복 글 묶기
- source·키워드 제외 필터
- 사용자 정의 HOT Score
- 여러 카드뉴스 템플릿
- 편집 상태 로컬 저장
- GPT를 통한 제목·캡션 작성 연결
- 이미지 생성 워크플로 연결
- Instagram 게시 자동화

이 항목들은 현재 필수 범위가 아니며, 실제 사용 후 필요성이 확인된 것만 구현한다.
